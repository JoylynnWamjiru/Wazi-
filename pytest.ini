[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    db: test needs a database (SQLite in-memory fixture; no Postgres required)
    pgvector: test needs real PostgreSQL + pgvector (run on the VPS only)
